Engine Isolation — правила внутри `engines/` 📌

Коротко:
- **Каждый движок** рисует только в своём internal `createGraphics`-буфере.
- API движка должен включать `init`, `execute`, `compose`, `dispose`.
- Нет доступа к `window.artLayer` или другим глобалям для рисования.

Проверка (быстрая команда):
  grep -nR "window.artLayer" . || true

Примеры:
- Файл движка: `engines/liquid-ink/engine.liquid-ink.js` — реализует `init/execute/compose` и рисует в `_buffer`.
- Загрузчик в `index.html` загружает `engine.<style>.js` (попытка именованного файла, затем fallback на `engine.js`).

Если нужен стандартный чек (CI) — добавьте простой grep-скрипт или pre-commit hook для `engines/`.
